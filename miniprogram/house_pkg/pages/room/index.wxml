<view class="room">
  <van-cell-group border="{{false}}">
    <van-cell wx:for="{{rooms}}" wx:key="*this" title="{{item.point}}{{item.building}}{{item.No}}" link-type="navigateTo" mark:room="{{item}}" bindtap="goForm" is-link />
  </van-cell-group>
</view>